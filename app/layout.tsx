import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
	variable: "--font-geist-sans",
	subsets: ["latin"],
});

const geistMono = Geist_Mono({
	variable: "--font-geist-mono",
	subsets: ["latin"],
});

export const metadata: Metadata = {
	title: "Robinson's Redesigns",
	description:
		"Landscaping and lawn care for homes and businesses in the DMV. Robinson's Redesigns boosts curb appeal and your property's exterior—healthy turf, tidy beds, outdoor spaces you'll love.",
	icons: {
		icon: "/logo.png",
		shortcut: "/logo.png", // IE/Edge/older browsers
		apple: "/logo.png",
	},
};

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="en">
			<body
				className={`${geistSans.variable} ${geistMono.variable} antialiased`}
			>
				{children}
			</body>
		</html>
	);
}
